var animation = bodymovin.loadAnimation({
    container: document.getElementById('top_animation'),
    renderer: 'svg', // 描画形式
    loop: true, // trueにしたらループ。1回再生の場合はfalse
    autoplay: true, // 自動再生
    path: './animations/top.json' // jsonのパスを指定
    });

var animation = bodymovin.loadAnimation({
    container: document.getElementById('step1_animation'),
    renderer: 'svg', // 描画形式
    loop: true, // trueにしたらループ。1回再生の場合はfalse
    autoplay: true, // 自動再生
    path: './animations/step1.json' // jsonのパスを指定
    });

var animation = bodymovin.loadAnimation({
    container: document.getElementById('step2_animation'),
    renderer: 'svg', // 描画形式
    loop: true, // trueにしたらループ。1回再生の場合はfalse
    autoplay: true, // 自動再生
    path: './animations/step2.json' // jsonのパスを指定
    });


var animation = bodymovin.loadAnimation({
    container: document.getElementById('step3_animation'),
    renderer: 'svg', // 描画形式
    loop: true, // trueにしたらループ。1回再生の場合はfalse
    autoplay: true, // 自動再生
    path: './animations/step3.json' // jsonのパスを指定
    });


var animation = bodymovin.loadAnimation({
    container: document.getElementById('step4_animation'),
    renderer: 'svg', // 描画形式
    loop: true, // trueにしたらループ。1回再生の場合はfalse
    autoplay: true, // 自動再生
    path: './animations/step4.json' // jsonのパスを指定
    });

var animation = bodymovin.loadAnimation({
    container: document.getElementById('footer_animation'),
    renderer: 'svg', // 描画形式
    loop: true, // trueにしたらループ。1回再生の場合はfalse
    autoplay: true, // 自動再生
    path: './animations/footer.json' // jsonのパスを指定
    });